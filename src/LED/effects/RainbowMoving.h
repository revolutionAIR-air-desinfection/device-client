#include "../../config.h"

int step = 0;

//! UNTESTED
void rainbowMoving()
{
    // for (int led = 0; led < NUMPIXELS; led++)
    // {
    //     // (360 / NUMPIXELS * led) calculates perfect HUE values for rainbow 
    //     pixels.setPixelColor(led, pixels.ColorHSV((360 / NUMPIXELS * (led + step)), 255, 255));
    // }
    // pixels.show();
    // step++;
    // delay(LEDstep_delay);
}
